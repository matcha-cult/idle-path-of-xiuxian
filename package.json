{
  "name": "idle-path-of-xiuxian",
  "version": "0.1.0",
  "private": true,
  "description": "放置·修仙之路 — ionet-ts + NestJS official example",
  "type": "module",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "dev:server": "pnpm --filter ./packages/server dev",
    "build": "pnpm -r run build",
    "build:server": "pnpm --filter ./packages/server build",
    "typecheck": "pnpm --filter ./packages/server typecheck"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
