/**
 * `@idle-path/ui-kit/testing` —— 仅测试用的辅助（不参与运行时，不进主 barrel）。
 */
export {
  DEFAULT_VIEWPORT_WIDTH,
  getViewportWidth,
  installViewportMock,
  resetViewport,
  setViewportWidth,
} from './viewport.js';
