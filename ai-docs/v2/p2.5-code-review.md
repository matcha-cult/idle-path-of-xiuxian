# P2.5 代码审阅报告 — 境界突破（自动审阅）

> 审阅日期：2025-09-12（编码完成后自动执行）
> 审阅对象：`src/modules/game/realm/*`、`src/common/config/app-config.ts`、`config/app.config.json`、README
> 审阅方式：静态走查 + 断言回归 + 并发竞态探测
> 结论：**发现 1 处 M 级并发成本漏洞已修复复验；P2.5 可验收。**

---

## 一、发现并修复

| 编号 | 级别 | 缺陷 | 修复 | 复验 |
| --- | --- | --- | --- | --- |
| F1 | M | 并发双突破可按**旧快照成本**重复扣费：两请求同读 realm=2/cost=800，先后 UPDATE 会把一次 800 扣成 realm=4（正确应为 800+1800） | UPDATE 增加 `realm = $快照` 守卫；守卫失败后重读区分 REALM_CHANGED（重试）与 LINGYUN_NOT_ENOUGH | 并发探测：双请求恰 1 成功，realm=3、lingyun 精确只扣 800，对侧受控失败 ✅ |

## 二、验证记录

| 项 | 结果 |
| --- | --- |
| typecheck / build | ✅ 零错误 |
| 状态：realm=1 铜皮，nextCost=200 | ✅ |
| 无灵韵突破 → LINGYUN_NOT_ENOUGH（消息含需/现有数值） | ✅ |
| grant 200 → 1→2 草根，剩余 0 | ✅ |
| 连冲 1→14：剩余灵韵与 200×n² 消耗表**逐境精确匹配**（836400） | ✅ |
| 14 境封顶 → MAX_REALM_REACHED（合道） | ✅ |
| 境界联动：realm=14 可装备此前被 TIER_TOO_HIGH 拒绝的 T2 物品 | ✅ |
| character/info 与突破结果一致性 | ✅ |
| 并发竞态：恰好一次升境、成本精确、对侧 REALM_CHANGED/LINGYUN_NOT_ENOUGH | ✅ |

## 三、登记（低优先级）

| 编号 | 项 | 处置 |
| --- | --- | --- |
| L1 | GET /breakthrough 与 character/info 存在字段重叠（realm/lingyun） | 观察；如前端认为冗余可合并为 realm 汇总接口 |
| L2 | 消耗表以数组入 config（可读性 OK）；若要动态曲线可后续改公式参数化 | 观察 |
| L3 | 突破无频率限制（正常玩法接口，无滥用面；确定性成功无需防刷） | 无需处理 |

## 四、值得肯定

1. 突破采用单条原子 UPDATE（扣费+升境一体），无跨库、无 SAGA 负担；
2. 成本校验走 WHERE 守卫而非读改写，天然免疫 TOCTOU；
3. F1 修复追加 realm 快照守卫后，并发语义收敛为「后至者重试或失败」，无重复扣费面；
4. 名称表（REALMS）与 v2 §2.1 完全一致；MAX_REALM 集中定义。

## 五、结论

**P2.5 通过自动审阅，可提交合并。**
