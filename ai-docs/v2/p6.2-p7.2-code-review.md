# P6.2 / P7.2 代码审阅 — 事件计数 + 演出编排

> 审阅对象：P6.2/P7.2 工作树（game_stat_counters/game_story_seen 两表 + StatModule + StoryModule + 三处钩子 + 任务新目标）
> 验证：typecheck / build / db:init:game(28 表) / 冒烟 23 断言 全绿

---

## 1. 审阅发现与处置

| 编号 | 级别 | 发现 | 处置 |
| --- | --- | --- | --- |
| M1 | M | 计数在结算成功后写入，与结算非原子 → 极端情况下少记（不会重复记） | 有意：偏向「不给玩家伪造进度」；计数仅影响任务推进，可补杀 |
| M2 | M | `kill_realm:<realm>` 等维度使计数行随 key 增长 | 量级可控（≤单位数+境界数）；后续可归档 |
| M3 | M | StoryService 每次请求内部调用 questService.list 获取任务状态（多次查询） | 接受；演出为低频读接口，后续可加缓存 |
| L1 | L | 瓦尔摧毁仍计 `craft_total`（消耗了通货，语义合理） | 有意 |
| L2 | L | 剧本仅文本 + 已读，无播放状态机/逐句推进 | 顺延前端 |
| L3 | L | `game_stat_counters.value` BIGINT → Number 精度债 | 继承登记 |
| L4 | L | 任务种子新增目标后，已按旧定义完成的任务进度保留 completed（不重开） | 合理的向后兼容；新目标只影响未完成角色 |
| L5 | L | `/story/seen` 不校验 nodeKey 是否属于已知节点（仅长度校验） | 接受（键为自描述字符串，误报无副作用） |

## 2. 关键正确性核对

- **击杀计数**：kill 7 次 u_r1_shanxiao 后，q_ch1_3 的 `kill_total` 目标 current 由 0 → 7（challenge/idle 亦走同一 recordKill 路径）。
- **突破计数**：breakthrough 后 q_ch3_1 的 `breakthrough_total` current=1。
- **炼器计数**：generate(凡品)+transmute 后 q_ch2_3 的 `craft_total` current=1。
- **演出编排**：`GET /story/chapter/1` 节点序 = chapter_intro → q_ch1_1 start/done → … → chapter_outro，文本与种子一致；`POST /story/seen` 后重查 seen=true 且重复标记幂等。
- **错误路径**：未知章节 CHAPTER_NOT_FOUND、未知任务 QUEST_NOT_FOUND、空 nodeKey INVALID_PARAM。
- **数据规模**：28 张表；`@Global` StatModule 注入 Unit/Craft/Realm/Quest；StoryModule 复用 QuestModule。

## 3. 验证命令与结果

```bash
pnpm --filter ./packages/server typecheck    # exit 0
pnpm --filter ./packages/server build         # exit 0
pnpm --filter ./packages/server db:init:game  # 28 张表
# 冒烟：23 断言 ALL P6.2/P7.2 SMOKE PASSED（计数/任务目标/剧本/已读/错误码）
curl -s localhost:3000/api/health             # 200 {"status":"ok"}
```

## 4. 结论

无 H 级缺陷；M1 的「少记不重复」策略已明确；M/L 归属清楚。P6.2/P7.2 可提交。
