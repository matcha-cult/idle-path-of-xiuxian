# 19 · 画布交互修复 + 视觉规格 v2（交给独立 session 执行）

> **用户反馈（2026-09-14，逐字）**：
> 「**你这个展现形式也是没有问题的，问题出在画布**，目前是 div+css
> 当点击『前往』，**界面直接白屏了**；当拖动界面，**界面直接飘逸到左边去了**，
> 拖动直接触发浏览器**选择文本搜索，搜索的内容是一串带空格数字**。
> 如果采用 canvas，会更麻烦吗。」
>
> **结论**：展现形式（视觉规格 v2）**用户已认可**；本轮 = **修画布交互 bug** + **落地视觉规格 v2**。

---

## 0. 根因（已读代码确认，不是猜测）

| # | 现象 | 根因 | 证据 |
| --- | --- | --- | --- |
| **B1** | 点「前往」→ **白屏** | **render 期未捕获异常**，而本仓**没有任何 ErrorBoundary** → React 卸载整棵树 = 白屏 | `grep ErrorBoundary packages/{web,ui-kit}/src` **零命中** |
| **B2** | 拖动 → **界面飘到左边** | **同一个根因**：未抑制原生拖拽 → 浏览器进入**文本选择**拖动，选择会**自动滚动最近的可滚动祖先**，于是整个界面被拖走 | 见 B3 |
| **B3** | 拖动 → 触发**选择文本搜索**，内容是**一串带空格数字** | `GraphCanvas` 容器有 `touchAction:'none'`（只管触屏），**没有 `userSelect:'none'`**，`onPointerDown` 也**没有 `preventDefault()`** → 鼠标拖动 = 浏览器原生选择。选中的那串数字正是**开发者网格的轴标 `0 1 2 3 … 20`** | `GraphCanvas/index.tsx:66-77` style 里只有 `touchAction`；`grep userSelect` **零命中** |

**B2 与 B3 是一个病**：把拖动抑制补上，两个一起消失。

---

## 1. 任务清单（逐项打勾，每项一个 commit）

- [ ] **T1 · 拖动抑制**（修 B2+B3）：`userSelect/WebkitUserDrag/draggable/preventDefault` — commit: —
- [ ] **T2 · ErrorBoundary**（让 B1 不再是白屏）：ui-kit 通用件 + 面板层包裹 — commit: —
- [ ] **T3 · 视觉规格 v2 · 名字上图 + 形状即层级** — commit: —
- [ ] **T4 · 视觉规格 v2 · 可交互靠饱和度（废掉虚线空心）** — commit: —
- [ ] **T5 · 文案中性**：战力门槛不再红绿判定 — commit: —
- [ ] **T6 · 画布占比 + 开发者网格默认关** — commit: —
- [ ] **T7 · 测试 + 四门禁** — commit: —

## 2. T1 · 拖动抑制（**先做这个，两个 bug 一起消失**）

`packages/ui-kit/src/layout/GraphCanvas/index.tsx`：

1. 容器 `style` 补（TS 里 `WebkitUserDrag` 需要 cast 或写进 `as CSSProperties`）：
   ```ts
   userSelect: 'none',
   WebkitUserSelect: 'none',
   WebkitUserDrag: 'none',
   ```
2. `onPointerDown`（在 `use-graph-viewport.ts`）**第一行加 `event.preventDefault()`**
   —— 阻止原生选择起步。容器没有 `tabIndex`、不需要聚焦，所以不会影响键盘可达性。
3. **拖动中光标变 `grabbing`**（`moved` 为真时），松手回 `grab`。
4. **枢纽 pin 加 `draggable={false}` + `onDragStart={(e) => e.preventDefault()}`**
   —— 防止「按在文字上拖」触发 HTML5 拖拽。
5. SVG 层也加 `userSelect: 'none'`（轴标是文字节点）。

**验收**：鼠标在画布上拖 1 秒，**不出现任何文本选中高亮**；页面不滚动；地图按 1:1 跟手平移。

## 3. T2 · ErrorBoundary（**把白屏变成可读报错**）

B1 是 render 期异常，但**本仓没有 ErrorBoundary**，所以我们连错误信息都拿不到。先补上：

- 在 `@idle-path/ui-kit` 加通用件 `ErrorBoundary`（**零业务**：`componentDidCatch` + `getDerivedStateFromError`），
  渲染 antd `Alert`（`type="error"`）+ 错误 message + 可折叠的组件栈；提供 `onError?` 回调。
  一文件一组件、≤200 行、同目录单测（hygiene 要求）。
- 在 `MapPanel` 外层（或 `pages/game` 的面板容器）包一层，`onError` 里
  **再额外打一条 `console.error`**，方便用户复制。
- **注意**：ErrorBoundary 是**诊断手段不是修复**。B1 的真因仍需定位 ——
  加了它以后白屏会变成一张写着错误信息的卡片，**请把那张卡片的文字发回来**。

## 4. T3 · 视觉规格 v2：**名字上图 + 形状即层级**

> 用户已认可这个方向：「你这个展现形式也是没有问题的」。
> 提议稿（真种子数据渲染）：`tmp/map-visual-proposal.mjs` → `tmp/map-visual-proposal.html`。

重写 `MapNodePin`：

| 环层 | 形状 | 尺寸 | 颜色（用 antd 语义 token，**禁 hex**） |
| --- | --- | --- | --- |
| `outer` 山门 | **圆角方形** | 30 | `token.colorInfo` |
| `peaks` 八峰 | **圆形** | 32 | `token.colorPrimary` |
| `inner` 四院 | **六边形** | 32 | `token.colorSuccess` |
| `summit` 主峰 | **八角星**（最大） | 40 | `token.colorWarning` |

- **名字写在形状下方**（不要塞进形状里 —— 「第八峰·历练」6 个字塞不下 64px 的圆）；
- 形状用内联 `<svg>` 画（`polygon` / `rect` / `circle`），**颜色只来自 `theme.useToken()`**；
- 当前所在：尺寸 ×1.15 + 外圈光晕；
- **旧口径作废**：`14-地图画布方案探讨.md` §11.1 第 2 条「图上不写名字」是**抄 PoE 抄错了前提**
  （PoE 靠**专属美术图标**识别，我们没有美术，必须靠文字）。请在代码注释里写明这条更正。
- `ICON_PX` / `MARKER_PX` 常量保留语义（热区仍 ≥44），但「视觉直径」改成**按环层分档**。

## 5. T4 · 视觉规格 v2：可交互靠**饱和度**，废掉虚线空心

现在 `disabled` = 灰 + **虚线** + 空心 + 透明 ⇒ 16/17 个点长这样 ⇒ 整张图**像未完成的线框图**
（虚线空心是「占位符」的视觉语言）。

改为：

| 状态 | 描边 | 底 | 名字 |
| --- | --- | --- | --- |
| 可交互（相邻 或 传送点已点亮） | 该环层颜色，**实线** 2.5px + 光晕 | 该色 20% 透明 | `token.colorText`，**可读** |
| 不可交互 | 该环层颜色 **去饱和**（`token.colorTextQuaternary`）实线 2px | `token.colorFillTertiary` | `token.colorTextTertiary`，**仍然可读** |
| 当前所在 | 该环层颜色 + 金色光晕 | 同可交互 | 加深 |

**虚线只保留给「未探明」这一个语义**（如果以后真有），**不要用来表达「不可交互」**。

## 6. T5 · 文案中性：战力不再是门槛

用户在 `tmp/Snipaste_2026-09-14_02-09-00.png` 里看到的
「战力门槛 **(仅参考)** 45/80 **战力不足 差 35**」**自相矛盾**：标签写了「仅参考」，文案却在喊「不足」，
还用了红色告警色。而战力限制**已经删除**（P2.0 §5：打不过进去送人头）。

改为：**「参考战力 80 · 我的战力 45」**，中性色（不要红绿判定）。
`StatCompare` 的 `okText` / `failText` 若仍要保留，改成中性措辞（如「高于参考」/「低于参考」），
**不得出现「不足」「差 N」这种失败语义**。

## 7. T6 · 画布占比 + 开发者网格默认关

- 宽屏下**画布区域 ≥ 62%**（现在右栏吃掉太多，21×21 的格子被压得很小）；
  窄屏（`<md`）仍按现有规则上下堆叠。
- **开发者网格默认改为关**（`resolveMapDebug` 的缺省从 `Boolean(env.DEV)` 改成 `false`），
  只保留 `?mapGrid=1` 显式打开。
  > 理由：它是**调坐标用的调试工具**，平时开着满屏点阵很吵，而且它的轴标文字正是 B3 里被选中的那串数字。
  > 这样正式服与开发服行为一致，你调坐标时加个参数即可。

## 8. T7 · 测试与门禁

- `GraphCanvas` 单测补：容器 `userSelect === 'none'`；`onPointerDown` 会 `preventDefault`；
  pin 的 `draggable === false`；
- `ErrorBoundary` 单测：子组件抛错 → 渲染错误卡片而不是白屏，且 `onError` 被调用；
- `MapNodePin` 单测：四种环层各自的形状标签（给形状加 `data-shape`）；名字**出现在 DOM 里**；
  不可交互**不是虚线**（断言 `stroke-dasharray` 不存在或 border-style 非 dashed）；
- **四门禁**：`pnpm -r run build` 0 错 / server `verify` 全绿 /
  ui-kit `test`（覆盖率阈值不破）/ web `test`（两个 `hygiene.test.ts` 不破）。
  ⚠️ ui-kit 的表单测试对机器负载敏感，**单独跑，不要和别的测试并发**（上一轮我在跑 e2e 时并发，
  4 个表单用例超时误报失败）。
- **`:3000` 后端不需要重启**（本轮只动前端与 ui-kit，服务端与协议零改动）。

## 9. 明确**不做**

- **不换 `<canvas>`**（理由见 §10）；
- 不改服务端、协议、`deriveEdges`、种子数据；
- 不改交互契约（点击只选中、移动走右栏按钮）；
- 不改 `game_map_objects` / `map.enter` / `map.waypoint`。

## 10. 为什么**不**换 canvas（供决策留档）

用户问「如果采用 canvas，会更麻烦吗」。**答案是：更麻烦，而且治不了这三个 bug。**

1. **架构本来就已经是「SVG + div」**：连线/网格是 SVG，枢纽是**绝对定位的 div**。
   换成 `<canvas>` 只会把连线那一层换掉。
2. **枢纽必须是 antd 组件** —— 这是用户最初的要求（「直接使用 antd 组件去渲染」）。
   `<canvas>` 里**塞不了 antd 组件**，只能自己画；那就等于放弃 antd 的主题、紧凑、无障碍。
3. **全 canvas 的额外成本**：字形度量与换行、HiDPI 缩放、命中测试、**无障碍**（canvas 对读屏器完全不透明，
   得再维护一份 DOM 列表）、主题 token 要手动取再喂给绘制调用、测试要从「断言 DOM」改成「mock canvas context」。
4. **这三个 bug 在 canvas 里照样存在**：canvas 上拖动**同样会选中周围文字**（浏览器按整个文档选区算）、
   **同样要 `preventDefault`**；平移数学也照样要写。
5. **canvas 真正的适用场景是几百上千个节点**的性能。我们现在 **17 个枢纽**，离那个门槛差两个数量级。

**结论**：该补的是**拖动抑制**，不是换渲染技术。
