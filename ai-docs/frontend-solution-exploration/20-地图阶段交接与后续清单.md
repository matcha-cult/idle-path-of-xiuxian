# 20 · 地图阶段交接与后续清单（2026-09-14）

> **用途**：本阶段的**唯一交接入口**。压缩上下文 / 新开会话 / 隔几天回来，只读这一篇即可接上。
> **状态**：地图阶段性**搁置**。功能全通、门禁全绿，但**用户对交互效果「非常不满意」**。

---

## 0. 一句话结论

地图做了四轮（P1 → P1.5 → P2.0 → P2.1），**每一轮的代码与门禁都是合格的**
（四条门禁我每轮都独立复跑过，见 §2），
但**用户对这个交互效果非常不满意**，所以地图先放一放。

> ⚠️ **最重要的缺口**：用户说「不满意」，但**不满意在哪个维度没有落盘**（见 §3）。
> 这是压缩上下文前**唯一真正有风险**的东西 —— 其余全部已在磁盘上。

---

## 1. 现在的地图长什么样（客观描述，不含评价）

| 项 | 现状 |
| --- | --- |
| **结构** | 17 枢纽 = 四门(4) + 八峰(8) + 四院(4) + 青云主峰(1) |
| **坐标** | 21 条交叉线（索引 `0..20`），主峰居中 `(10,10)`；`cellPx = 48` |
| **布局** | 八卦式：八峰逆时针编号、整体旋转 22.5°（正北让给北门）；四院在四正方位；一条正轴 = 山门→院→主峰 |
| **渲染** | SVG 画连线/网格 + 绝对定位 `div` 画枢纽；支持缩放平移；**枢纽恒定屏幕尺寸** |
| **视觉** | **形状即层级**（门=圆角方 / 峰=圆 / 院=六边形 / 主峰=八角星最大）；**名字写在形状下方**；可交互靠**饱和度**区分 |
| **移动规则** | 相邻（有边）可直接前往（**无战力门槛**）；不相邻需传送，传送点须已点亮；不可交互=暗色 disabled |
| **交互契约** | **点击枢纽只选中、绝不移动**；移动走右栏「前往此地」；PC 另支持双击直达 |
| **对象层** | 4 院 + 主峰挂 **11 个职能对象**（藏经阁/传功崖/丹霞院/百器阁…），本轮**只做显示+入口**，未做交互 |
| **开发者网格** | 默认**关**，`?mapGrid=1` 打开 |

---

## 2. 已交付（四轮，含我的独立复核结论）

| 轮 | 内容 | 关键 commit | 复核 |
| --- | --- | --- | --- |
| **P1** | 21×21 交叉线坐标系 + ui-kit `GraphCanvas`（缩放平移·恒定尺寸枢纽·开发者网格）+ 左画布右详情 | `de06b7b`…`34b944a` | ACCEPT（四条门禁复跑一致） |
| **P1.5** | 全量显示所有节点 + `discovered` 闸门 | `0d4bab8`…`affbdbb` | **已回滚**（方案改了），代码留在分支 `p1.5-wip` |
| **P2.0** | 17 枢纽重写（四门八峰四院主峰）+ 坐标 21→20 条线 + `game_map_objects`(11) + `game_map_state` + **相邻闸门** + **删战力限制** + 边表**几何派生** | `5f01e2a`…`e6e6050` | ACCEPT |
| **P2.1** | 拖动抑制 + `ErrorBoundary` + **视觉规格 v2** + 文案中性 + 白屏修复（移动改静默刷新） | `96090e0`…`9e946d2` | ACCEPT（含**变异测试**：旧实现会让守门测试失败） |
| **P3.0** | **第八峰·历练峰在线打怪升阶** + 怪物数据分层（`level/threshold` 只留秘境） | `790fa9f`…`532350c` | ACCEPT（含**前后对照**：旧 :3000 上 e2e 2 failed → 重启后 3 passed） |

**P2.0/P2.1 的实测数字**：17 节点 / 32 边 / 11 对象 / **连线交叉数 0**（旧拓扑 9）/ 最长边 7 格（旧 19）/
新角色 `adjacent=true` 恰 4 个（四门）/ 从北门可达 **17/17**。
**门禁**：build 0 错 · server **1497/1497** · ui-kit **433/433**（覆盖 99.61/95.43/97.33）· web **568 passed**。

---

## 3. ⚠️ 未落盘的一条：用户「不满意」的具体所指

**这是压缩上下文前唯一真正的风险。** 现在只知道结论（「非常不满意」），不知道维度。
下一轮若不知道这一点，**极可能再重蹈一遍**。

已知的四轮反馈线索（都是**现象**，不是**诉求**）：

1. 最初「体验特糟糕」（因为只显示 4 个孤点）→ 已改成全量显示；
2. 「点前往白屏 / 拖动飘到左边 / 拖动选中文本」→ 已修；
3. 「这个地图也是不符合你预期的」→ 于是做了视觉规格 v2；
4. 现在：「**总的交互效果非常不满意**」。

**✅ 用户已给出判定（2026-09-14）**：

> 「地图交互效果这个不满意不好给你具体描述。**这个是当前技术选型的问题，根本原因是当前实现技术问题**，
> 当前 **div+css 的实现模式，很难实现我想要的放大缩小拖拽功能**。而且交互过程的漂移、加载过程白屏，
> 这些都是没法给你截一张图表示的。**此处跳过优化是否能进入第八峰历练峰开发？**」

**结论：地图交互优化【跳过】**。用户判定这是**技术选型层面**的问题（不是靠继续修 bug 能解决的），
且漂移/白屏这类**过程性缺陷无法用截图表达**。**不再追问维度，不再重做地图交互。**

> 留档（供将来真要重做时参考）：`div+css` 的手感劣势主要在**平移惯性与多点触控缩放**；
> 若以后要重做，三条路是 ① 换成熟的手势/变换库 ② 混合（canvas 画底图与连线 + DOM 放枢纽）
> ③ 全 canvas（代价见 `19-...md` §10：antd 组件塞不进 canvas、无障碍要另维护一份 DOM、测试要 mock canvas）。
> **本轮不选任何一条，按用户指示跳过。**

**以下候选维度记录备查，但已不再追问：**

| # | 候选 | 具体表现 |
| --- | --- | --- |
| A | **点选/前往的流程**太笨 | 「点一下只选中、还得再去右栏点前往」是不是太绕？想不想「点一下就预览、再点一下就走」？ |
| B | **缩放平移手感** | fit 视图太小/太大、拖动手感、滚轮规则（现在是 Ctrl+滚轮） |
| C | **信息密度** | 格子太小、名字重叠、17 个点散在大片空白里 |
| D | **没有「地图感」** | 只有点和线，没有地形/道路/底图 → 想不想要 `background_key` 底图（P4） |
| E | **反馈不足** | 移动没有动画/位移感、到达没有表现 |
| F | **整体方向就不对** | 也许根本不该是一个「可缩放画布」，而该换个形态 |

---

## 3.5 ⚠️ 当前方向（2026-09-14 晚，**最新，压过本文其余部分**）

> ✅ **本轮 8 个问题已由用户全部拍板并已交付**：见 `22-秘境解锁体系设计任务书.md`
> （含 §13 执行记录：T1~T9 状态、发现并修掉的 3 个真实缺陷、全部门禁与真后端 e2e 实测数据）。
> 下面这张问题表**已关闭**，仅作留档；下文「与 P3.0 的冲突」也已由 `22-...md` §6.3 / §8 给出结论。
>
> **一句话交付**：秘境按境界分档为 13 条记录（与地图节点彻底解耦），
> 由第八峰·后山的「秘境石台」对象交互突破；在线打满 3 层即突破并自动退出，
> 已突破的才进秘境页面；挂机 = 已突破 ∧ `idle_allowed`，在线战斗期间强制关闭。

> **用户指令（逐字）**：
> 「现在有个问题，还是得返回修改地图节点，就是**第八峰后山，应该是解锁宗门秘境的入口，
> 而不是挂机秘境的入口**。**所有已解锁的秘境，都会在秘境页面显示**。
> 现在请**删除现有秘境**，开始**策划秘境解锁**。这是一个很多细节的内容，
> 你先评估是否可以压缩上下文，压缩之后再开始设计。」

### 这次要改的三件事

| # | 内容 |
| --- | --- |
| **1** | **第八峰·后山**的语义从「**挂机秘境本体**」改为「**宗门秘境的解锁入口**」（一个地点，不是一个副本） |
| **2** | **已解锁的秘境统一在「秘境页面」显示**（不再靠地图节点承载） |
| **3** | **删除现有 6 个秘境，重做「秘境解锁」体系** —— 细节多，**先设计再动手** |

### 现有 6 个秘境（将被删除/重做）

```
zone_qingyun 青云山脚  minRealm1  maxFloor20  unit=u_r1_shanxiao
zone_miwu    迷雾秘境  minRealm4  maxFloor20  unit=u_r4_xueyan
zone_guhai   古海遗迹  minRealm7  maxFloor20  unit=u_r7_jiaomo
zone_dajie   大劫战场  minRealm10 maxFloor20  unit=u_r10_guiwang
zone_hundun  混沌之隙  minRealm13 maxFloor20  unit=u_r13_feishengmo
zone_houshan 后山历练  minRealm4  maxFloor3   unit=u_r5_tongmo
```
> 其中 5 个是**旧模型遗留**（maxFloor 20 / 按境界分档的挂机场），
> 只有 `zone_houshan` 是 P3.0 刚做实的（3 层 / 75·87·99 / bossEveryFloors=3）。
> **注意**：P3.0 的在线探索 tick 与离线结算**都挂在 zone 上**，删秘境会牵动它们（见下）。

### 设计必须先定的问题（**这是「很多细节」的清单**）

| # | 问题 | 备注 |
| --- | --- | --- |
| 1 | **宗门秘境是什么形态？** 按境界分档的系列？还是不同类型（炼丹/剑冢/藏经/试炼…）？ | 现有 6 个是按境界分档 |
| 2 | **解锁条件是什么？** 现模型是「境界 ≥ minRealm + 前置秘境 bestFloor ≥ N」（链式） | 要改成战绩/道具/剧情/宗门贡献？ |
| 3 | **解锁动作在哪发生？** 到第八峰·后山 → 与某个对象交互 → 解锁？还是满足条件自动解锁？ | 若走对象层，需 `map.interact`（P2.1 遗留未做） |
| 4 | **未解锁的秘境在秘境页面显示吗？** 锁定态+条件？还是完全不显示？ | 影响「探索欲」 |
| 5 | **挂机与秘境的关系**：每个秘境都能挂机？还是只有部分？ | **D9「挂机只能在历练秘境峰」这条要改吗？** |
| 6 | **P3.0 刚做的在线 tick 怎么办？** 它现在是「进秘境后在线自动推进」 | 新模型下是否保留？ |
| 7 | **境界跨度**：青云宗秘境覆盖几境？ | D10 说青云宗历练到**第五境** |
| 8 | **6 个全删还是保留骨架？** `zone_houshan` 的三层（75/87/99）刚做实 | 删了要重推数值 |

### 与 P3.0 的冲突（必须一起考虑）

P3.0 刚交付并验收通过，它把「**在线打怪升阶**」挂在了 `zone` 上（`zone.enter` 写入 `game_zone_state`
→ 在线 tick 推进层数 → 首个 Boss 解锁离线挂机）。
**删秘境 / 改入口语义会直接牵动它**，所以设计时要明确：
**P3.0 那套「在线推进 + 层数 + 解锁离线挂机」是保留、迁移还是重做。**

---

## 4. 未决事项（都不阻塞，但悬着）

### 4.1 待用户拍板
| # | 事项 | 备注 |
| --- | --- | --- |
| 1 | 四院命名与职能分配（传法院/育灵院/百工院/执法院 + 四象五行配方位） | 只改种子 |
| 2 | 主峰三对象的 `feature_key`（现：护法堂=`waypoint`、内门广场/执事堂=`quest`） | 只改 `map-objects.json` |
| 3 | **点阵大小**：瓶颈是 `CANVAS_HEIGHT=520`（`zoom_fit` 取短边），放宽右栏只增加留白 | 调高度会重触发「面板短边 ≥462px」约束 |
| 4 | **17 个名字在密集处可能重叠** | 可选：按 zoom 隐藏名字 / 碰撞避让 |

### 4.2 已知潜伏隐患（**建议尽早修**）
- **`map.service.panel()` 里的 `discovered` 过滤器仍在**（`map.service.ts:253`）。
  今天恰好空转 —— 因为 17 个节点的 `requires_node_code` **全是 null**，`discoveredCodes` 判全员已发现。
  **一旦有人用这个字段做剧情前置（它正是为此保留的），那个节点会从大地图上悄悄消失**，
  与「直接显示所有点」直接冲突。修法：过滤改成全量下发，`discovered` 只作显示标记。

### 4.3 未接线
- **对象「已实现系统 → 跳既有面板」缺导航层**：本仓没有面板切换 API。
  现在未实现的走 `FeatureGate`（禁用）、已实现的只给一个「已开放」标签。

---

## 5. 后续清单（按优先级）

### A. 地图交互重做 —— **阻塞在 §3**（等用户指认维度）
### B. 地图功能缺口（方向明确，随时可开）
| # | 内容 | 规格在哪 |
| --- | --- | --- |
| B1 | **对象层交互**：`map.interact` + 对象进度表（点了 NPC 发生什么） | `18-...md` §3 已留口 |
| B2 | **P4 底图**：建 `packages/web/public/maps/` + `background_key` 填值 + 注册表 | `14-...md` §3.6 |
| B3 | **地图编辑器**（种子工具 T1）：拖点 + 铺底图 + 导出种子 | `ai-docs/种子工具开发需求清单.md` T1 |
| B4 | **P3 动态**：角色令牌逐格移动、迷雾、怪物 tick 可视化 | `14-...md` §7 |

### C. 其它未开工（都是老账）
| # | 内容 | 出处 |
| --- | --- | --- |
| C1 | `settings-revision-2.md` §7.7 的 **44 项待明确**（与下一张图「山下凡尘」相关的优先） | §7.7 |
| C2 | **七职业与功法/道基流派的关系**（§7.7 里长期悬着的一条） | §7.7 |
| C3 | 第 3/4/5 章的两张地图（山下凡尘 / 混沌裂隙）**还没定义** | §5.6 / §7.0 |
| C4 | `drop_tables` 里 `dt_band1..5` 仍是旧章节名 | 已知残留 |

### D. 工程债
| # | 内容 |
| --- | --- |
| D1 | `discovered` 过滤器隐患（§4.2）—— 一行 + 一条测试 |
| D2 | 对象跳转所需的**导航层**（§4.3） |
| D3 | **ui-kit 表单测试对机器负载敏感**：并发跑会假失败（ActionForm/SelectField/SwitchField/TextField 超时），**单独跑就全绿** |

---

## 6. 本阶段文档索引（按阅读顺序）

| 文档 | 是什么 |
| --- | --- |
| `11-面板重做进度与接续手册.md` | 面板层的总纪律（§4「组件能抽就抽」是硬约束） |
| `13-地图面板实施任务书.md` | 地图面板第一版（已被后续取代，看 §8~§10 的阶段记录） |
| `14-地图画布方案探讨.md` | **方案总纲**：§14 坐标系（权威）、§12 交互契约、§11 PoE 参照、§3.6 底图 |
| `15-P1画布实施任务书.md` | P1 任务书（已交付） |
| `16-P1.5全量显示任务书.md` | **已作废**（基于 `discovered` 闸门，被 18 取代） |
| `17-青云宗八卦布局规则.md` | 布局规则 + 用户全部拍板记录 |
| `18-P2.0八卦重做与相邻移动任务书.md` | P2.0 任务书（已交付）：17 枢纽 / 相邻移动 / 对象层 |
| `19-画布交互修复与视觉规格v2任务书.md` | P2.1 任务书（已交付）：拖动抑制 / ErrorBoundary / 视觉 v2 |
| `21-P3.0历练峰在线打怪升阶任务书.md` | P3.0 任务书（已交付）：在线打怪升阶 tick + 实时面板 |
| `22-秘境解锁体系设计任务书.md` | **当前方向**：删 6 个旧秘境、重做「秘境解锁」（13 秘境 / 3 层 / 突破即解锁 / 在线与挂机互斥）。**待用户拍板 §10 的 🔴Q1~Q4 后开工** |
| `ai-docs/种子工具开发需求清单.md` | 种子工具需求（T1 = 地图编辑器） |
| `user-docs/青云宗八卦布局图.png` | 用户手绘的设计输入 |
| `user-docs/青云宗地图构想.md` | 更早的文字版构想（四门八峰·内环功能·中央主峰） |

---

## 7. 环境现状（压缩后要重新确认，别假设）

- **前端 dev server** 与 **后端 `dist/main.js`** 是受管后台任务（压缩后**用 `job_list` 重新确认**，
  不要假设端口上跑的是谁）。开发下 vite 把 `@idle-path/ui-kit` 别名指到 `src`，所以改源码即生效。
- **数据库已被重灌**（配置表按新种子：17 节点 / 32 边 / 11 对象 / grid 20×20），
  备份在 `/tmp/idle_game_backup.dump`（**容器重启会丢**）。
- **7 个提交未 push**（领先 `origin/dev`）。`tmp/` 全部未提交（gitignore）。
- 分支 `p1.5-wip` 保留着已回滚的 P1.5 实现，需要时可用。
